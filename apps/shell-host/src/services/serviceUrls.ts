export {
  BASE_SERVICE_ENDPOINTS,
  PROVIDER_ENDPOINTS,
  TOKEN_AUTH_ENDPOINTS,
  buildAuthServiceUrl,
  buildBaseServiceUrl,
  buildProviderUrl,
  isTokenAuth,
  isTokenAuthMode,
} from "@jaldee/shared-modules";
